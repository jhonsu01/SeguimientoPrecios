<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base (pre-Compose): fondo oscuro para splash y barras del sistema. -->
    <style name="Theme.SeguimientoPrecios" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/brand_bg</item>
        <item name="android:navigationBarColor">@color/brand_bg</item>
        <item name="android:windowBackground">@color/brand_bg</item>
    </style>
</resources>
